import styled from 'styled-components';

export const BoxTituloTutorial = styled.div``;

export const TituloCalculadorap = styled.h1`
  font-family: var(--fonte-principal);

   @media (max-width: 440px) {
    font-size: medium;
    text-align: center;
  }
`;